{
  "name": "studio-inventory",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "test": "for f in tests/*.mjs; do node \"$f\"; done",
    "cap": "cap",
    "android:sync": "npm run build:web && cap sync android",
    "apk:debug": "npm run build:web && cap sync android && cd android && ./gradlew assembleDebug && cd .. && cp android/app/build/outputs/apk/debug/app-debug.apk studio-inventory-debug.apk"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "jsqr": "^1.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0"
  }
}
