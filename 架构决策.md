# 工作室物品管理 - 架构决策

## 当前结论

手机端主线切换为 **原生 Android**。

```text
Kotlin + Jetpack Compose + CameraX + ML Kit + JSON snapshot
```

新工程目录：

```text
mobile_android/
```

包名：

```text
studio.inventory.android
```

旧的 Web/Capacitor 和 Flutter 手机实现不再作为继续开发方向。相关代码可以保留作历史参考，但路线文档已删除，避免后续误判主线。

## 为什么改成原生 Android

这个项目近期只要求先做好 Android 手机端，核心体验是高频扫码。

原生 Android 的优势更直接：

- 页面内嵌相机预览更可控。
- CameraX、手电筒、震动、声音、权限和生命周期少一层桥接。
- 不依赖第三方 Flutter 扫码插件稳定性。
- 现有业务很轻，重写 Kotlin 业务规则成本低。
- 第一版只需要本地 JSON，不需要跨端同步架构先行。

## 第一版范围

只做手机本地闭环：

```text
生成固定二维码标签
扫物品/重量/库位码
按钮确认入库/出库/盘点/归档
库位整理模式连续扫码自动更新库位
本地 JSON 保存
库存搜索和流水查看
撤销上一笔写操作
```

不做：

```text
WebDAV 自动同步
SQLite/Room
正式签名
蓝牙打印
Docker 服务端
PWA
自然语言助手
多设备协作
```

这些放到流程跑通后再加。

## 数据原则

二维码是固定信息来源，本地库存 JSON 是查询和库存事实来源。

```text
扫物品码
  -> 只展示固定信息
  -> 不写本地数据

点入库
  -> 写 item
  -> item 内保存固定字段副本
  -> 保存 current_g/current_qty/location/status 等变量
```

本地不保存纯标签草稿；只保存已入库或曾经入库/出库/归档过的物品。

## 扫码原则

第一版只支持 `v1;`，不兼容旧短码和 `msi:v1;`。

扫码顺序可以自由，但写操作默认要按钮确认。

唯一例外是“整理库位”模式：

```text
扫库位码
点整理该库位
连续扫已入库物品
每扫一个自动更新 location
```

## 后续演进

当 Android 第一版流程跑通后，再考虑：

1. 把 JSON snapshot 一条条迁入 SQLite/Room。
2. 接正式 release 签名。
3. 做导出/导入和备份。
4. 接 WebDAV 或其他同步。
5. 做打印能力。
6. 再评估 Web/PWA/服务端是否值得恢复。
