# 0.4.1 交互层重写

## 重写边界

保留：

```text
Kotlin / Jetpack Compose
CameraX + ML Kit
v1 二维码协议
SQLite 四表
德佟打印 SDK
标签生成和打印
```

重写：

```text
扫码会话状态
扫码后的确认和等待
模式切换
冲突和替换
扫码页布局
库存详情低频操作
```

## 新状态模型

扫码页只维护一个 `ScanWorkflowState`：

```text
mode
item
weightG
quantity
location
sortingLocation
review
```

`review` 表示当前正在等待用户确认的单次输入：

```text
Item
Weight
Location
ModeSwitch
```

二维码识别后不会直接写入扫码会话。流程是：

```text
识别二维码
暂停相机分析
打开确认层
用户确认 -> 写入扫码会话 -> 恢复相机
用户取消 -> 会话不变 -> 恢复相机
```

## 已实现规则

```text
更新库存和绑定库位只能选择已经入库的物品
入库模式不能重复入库当前已在库物品
已归档物品不能重新入库、盘点、绑定或整理库位
替换物品时清空旧物品对应的重量、数量和库位
替换重量或库位时必须明确确认
耗材毛重必须大于空盘重量
零件必须有单重才能由重量计算数量
未知库位必须确认名称
未完成流程切换模式时必须确认清空
库位整理模式仍允许连续扫码自动更新库位
库位整理重复扫描已在目标库位的物品时跳过，不重复写库位
离开扫码页时释放 CameraX 分析线程和 ML Kit 扫描器
```

底部当前流程按模式和物品类型只显示必要字段；整理库位时只显示目标库位、最近结果和结束操作。

## 库存详情

库存项点击后展示：

```text
固定字段
当前状态
当前库位
毛重 / 空盘 / 可用重量
总重 / 单重 / 数量
入库、出库和更新时间
最近三条主流水
```

出库和归档从详情层触发，并再次确认后写入。

## 验证状态

已完成：

```text
./gradlew assembleDebug
./gradlew testDebugUnitTest
Kotlin 编译
APK 打包
旧扫码 UI 和旧冲突状态已从主代码删除
Android 12 MuMu 模拟器完成耗材、零件、其他入库
完成更新库存、绑定库位、整理库位、重复扫码跳过和出库验收
完成模式切换保留/清空、详情、流水和 20 秒自动暂停验收
零件单重 0.42g 保持精度，420g 正确换算为 1000 件
SQLite 日常写入改为逐表增量 upsert/append
库存搜索固定字段、备注、库位及类型/状态组合条件已有单元测试
流水页仓库地址可点击并拉起系统浏览器
```

仍需实体设备完成：

```text
实体手机相机画面和扫码速度复核
大字体与软键盘下确认表单布局复核
40x30mm 标签真实打印和纸面二维码可扫性
```

真机测试重点：

```text
扫到二维码后是否只弹一次确认层
确认期间相机是否停止重复识别
确认/取消后相机是否自动恢复
未完成流程是否稳定保留在底部
切换模式是否会先询问是否清空
小屏和软键盘下表单是否可滚动
库存详情出库/归档确认是否正确
```

完整验收步骤见 `MANUAL_TEST_MATRIX.md`。0.4.1 的库存闭环已通过模拟器验收，打印实测和实体手机布局复核不阻塞当前测试版发布。
